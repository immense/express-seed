extends ../layout

block page-header
  h1 Editing User
  +back-link("users", "/users")
  
block content
  include form
  
block script
  script
    :coffee
      $ -> window.view = new UserFormView '#{id}'
